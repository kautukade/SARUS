name: Jubi Production Readiness

on:
  push:
    branches:
      - main
      - 'agent/**'
      - 'fix/**'
    paths:
      - 'jubi/**'
      - 'sarus/**'
      - 'config/**'
      - 'installer/**'
      - 'driver/**'
      - 'tests/**'
      - 'BUILD_MANIFEST.json'
      - 'docs/**'
      - 'scripts/**'
      - 'package*.json'
      - '*.bat'
      - '*.pyw'
      - '.github/workflows/**'
  pull_request:
    paths:
      - 'jubi/**'
      - 'sarus/**'
      - 'config/**'
      - 'installer/**'
      - 'driver/**'
      - 'tests/**'
      - 'BUILD_MANIFEST.json'
      - 'docs/**'
      - 'scripts/**'
      - 'package*.json'
      - '*.bat'
      - '*.pyw'
      - '.github/workflows/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  linux-production-gates:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Compile Python
        run: python -m compileall -q jubi sarus tests scripts
      - name: Functional and live HTTP regression tests
        run: |
          python tests/jubi_functional_regression_test.py
          python tests/jubi_http_functional_test.py
      - name: Installer lifecycle static tests
        run: python tests/jubi_installer_lifecycle_test.py
      - name: Validate dashboard JavaScript syntax
        run: |
          node --check sarus/web/assets/app.js
          node --check sarus/web/assets/knowledge.js
          node --check sarus/web/assets/council.js
          node --check sarus/web/assets/research.js
          node --check sarus/web/assets/network.js
          node --check sarus/web/assets/vision.js
          node --check sarus/web/assets/operator.js
          node --check sarus/web/assets/shell-extension.js
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - name: Dashboard DOM integration tests
        run: |
          npm ci --ignore-scripts --no-audit --no-fund
          npm test
      - name: Unified dashboard regression tests
        run: python tests/dashboard_ui_test.py
      - name: Advanced Local Brain tests
        run: python tests/jubi_brain_test.py
      - name: Provider Manager tests
        run: python tests/jubi_provider_manager_test.py
      - name: Semantic Knowledge and Experience tests
        run: python tests/jubi_knowledge_test.py
      - name: AI Council and Supervisor tests
        run: python tests/jubi_council_test.py
      - name: Public Web Research tests
        run: |
          python tests/jubi_research_test.py
          python tests/jubi_research_ui_test.py
      - name: Computer operator, LAN and Vision tests
        run: |
          python tests/jubi_operator_test.py
          python tests/jubi_network_test.py
          python tests/jubi_vision_test.py
          python tests/jubi_network_vision_ui_test.py
      - name: Jubi Phase 0 regression tests
        run: python tests/jubi_phase0_test.py
      - name: Production readiness tests
        run: python tests/production_readiness_test.py
      - name: Fable integration tests
        env:
          SARUS_RECEIPT_SIGNING_KEY_FILE: ${{ runner.temp }}/sarus-fable-receipt.key
        run: python tests/fable_integration_test.py
      - name: Foundation integration regression
        env:
          SARUS_BROKER_APPROVAL_SECRET: ci-only-approval-secret-not-for-production
          SARUS_RECEIPT_SIGNING_KEY_FILE: ${{ runner.temp }}/sarus-regression-receipt.key
        run: python tests/integration_test.py
      - name: Broker security tests
        env:
          SARUS_BROKER_APPROVAL_SECRET: ci-only-approval-secret-not-for-production
          SARUS_RECEIPT_SIGNING_KEY_FILE: ${{ runner.temp }}/sarus-security-receipt.key
        run: |
          python tests/broker_security_test.py
          python tests/ring0_bridge_test.py

  windows-production-gates:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Validate PowerShell syntax
        shell: powershell
        run: |
          $scripts = @(
            'installer\JUBI-PREREQUISITES.ps1',
            'installer\JUBI-BACKGROUND.ps1',
            'installer\REGISTER-JUBI-BACKGROUND.ps1',
            'installer\UNREGISTER-JUBI-BACKGROUND.ps1',
            'installer\BUILD-LAUNCHER.ps1',
            'scripts\VERIFY-SARUS-REPO.ps1',
            'installer\INSTALL-SARUS.ps1',
            'installer\EXE-INSTALL.ps1',
            'installer\SETUP-BROKER.ps1',
            'installer\CERTIFY-SARUS.ps1',
            'installer\SIGN-RELEASE.ps1',
            'installer\UNINSTALL-SARUS.ps1',
            'driver\SarusRing0\BUILD-RING0.ps1',
            'driver\SarusRing0\INSTALL-RING0.ps1'
          )
          foreach ($script in $scripts) {
            $tokens = $null
            $errors = $null
            [void][System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path $script), [ref]$tokens, [ref]$errors)
            if ($errors.Count -gt 0) {
              throw "$script syntax errors: $(($errors | ForEach-Object Message) -join '; ')"
            }
          }
      - name: Compile source launcher and verify core payload
        shell: powershell
        run: .\scripts\VERIFY-SARUS-REPO.ps1
      - name: Compile Python
        run: python -m compileall -q jubi sarus tests scripts
      - name: Functional and live HTTP regression tests
        run: |
          python tests/jubi_functional_regression_test.py
          python tests/jubi_http_functional_test.py
      - name: Installer lifecycle static tests
        run: python tests/jubi_installer_lifecycle_test.py
      - name: Validate dashboard JavaScript syntax
        run: |
          node --check sarus/web/assets/app.js
          node --check sarus/web/assets/knowledge.js
          node --check sarus/web/assets/council.js
          node --check sarus/web/assets/research.js
          node --check sarus/web/assets/network.js
          node --check sarus/web/assets/vision.js
          node --check sarus/web/assets/operator.js
          node --check sarus/web/assets/shell-extension.js
      - name: Unified dashboard regression tests
        run: python tests/dashboard_ui_test.py
      - name: Advanced Local Brain tests
        run: python tests/jubi_brain_test.py
      - name: Provider Manager tests
        run: python tests/jubi_provider_manager_test.py
      - name: Semantic Knowledge and Experience tests
        run: python tests/jubi_knowledge_test.py
      - name: AI Council and Supervisor tests
        run: python tests/jubi_council_test.py
      - name: Public Web Research tests
        run: |
          python tests/jubi_research_test.py
          python tests/jubi_research_ui_test.py
      - name: Computer operator, LAN and Vision tests
        run: |
          python tests/jubi_operator_test.py
          python tests/jubi_network_test.py
          python tests/jubi_vision_test.py
          python tests/jubi_network_vision_ui_test.py
      - name: Jubi Phase 0 regression tests
        run: python tests/jubi_phase0_test.py
      - name: Production readiness tests
        run: python tests/production_readiness_test.py
      - name: Fable Windows tests
        env:
          SARUS_RECEIPT_SIGNING_KEY_FILE: ${{ runner.temp }}\sarus-fable-receipt.key
        run: python tests/fable_integration_test.py
      - name: Provision broker test keys
        shell: powershell
        run: |
          .\installer\SETUP-BROKER.ps1
          $secret = Join-Path $env:LOCALAPPDATA 'SARUS\broker\approval.secret'
          $receipt = Join-Path $env:LOCALAPPDATA 'SARUS\broker\receipt-signing.key'
          "SARUS_BROKER_SECRET_FILE=$secret" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
          "SARUS_RECEIPT_SIGNING_KEY_FILE=$receipt" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
      - name: Broker and Ring0 Windows tests
        run: |
          python tests/broker_security_test.py
          python tests/ring0_bridge_test.py
